<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication" >

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
         >

        <activity
            android:name=".glide.GlideTestActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MvvmTestActivity"
            android:exported="true" />
        <activity android:name=".retrofit.RetrofitActivity" />
        <activity
            android:name=".navigation1.ActivityNavigation1"
            android:label="@string/title_activity_navigation1" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity android:name=".ButtonNavigation.ButtonNavigationActivity" />
        <activity android:name=".room.RoomActivity" />
        <activity android:name=".lifecycles.LifeCyclesActivity" />
        <activity android:name=".calculate.CalculationTestActivity" />
        <activity
            android:name=".navigation.NavigationActivity"
            android:label="@string/title_activity_navigation" />
        <activity android:name=".vmss.ViewModelSaveStateActivity" />
        <activity android:name=".CountActivity" />
        <activity android:name=".DataBindingActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CalculatorActivity" />
    </application>

</manifest>